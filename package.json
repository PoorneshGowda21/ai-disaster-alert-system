{
  "name": "ai-disaster-alert-system-root",
  "version": "1.0.0",
  "description": "Orchestrates execution of the AI Disaster Alert System MERN stack workspace",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend/frontend-pwa",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend/frontend-pwa",
    "model": "python model-service/app.py",
    "dev": "npx concurrently --kill-others \"npm run backend\" \"npm run frontend\" \"npm run model\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
